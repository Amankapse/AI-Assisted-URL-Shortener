# AI Traceability Matrix

| Requirement ID | Prompt ID | AI output | Decision | Human modification | Validation |
|---------------|-----------|-----------|----------|--------------------|------------|
| AI-000 | P-000 | Phase 0–1 planning documents | Accepted | Edited for assignment alignment | Document review |
| AI-001 | P-001 | Corrected Phase 1 POM dependency management and removed invalid Testcontainers Redis artifact | Edited | Removed invalid `org.testcontainers:redis` dependency and moved Testcontainers BOM to dependencyManagement | `./mvnw clean verify`, `./mvnw dependency:tree` |
| AI-002 | P-013 | Phase 2 validation stabilization for Docker/Testcontainers and Flyway PostgreSQL startup | Accepted | Human approved adding `org.flywaydb:flyway-database-postgresql`; test profile changed from Hibernate update to validate | `.\mvnw.cmd clean verify` passed; `.\mvnw.cmd dependency:tree` confirmed `flyway-core:11.7.2` and `flyway-database-postgresql:11.7.2`; logs confirmed PostgreSQL Testcontainers startup, Flyway V1 application, Hibernate schema validation, and complete current test suite execution |
| AI-003 | P-014 | Expanded Phase 2 validation tests and fixed defects found after Flyway/Testcontainers startup | Accepted | Kept owner enforcement behind `CurrentOwnerProvider`; kept production APIs free of owner input; updated Springdoc from 2.2.0 to 2.8.6 for Spring Boot 3.5 compatibility; no migrations or Phase 3 features added | `.\mvnw.cmd clean verify` passed with 41 tests; `.\mvnw.cmd dependency:tree` confirmed Flyway modules at 11.7.2 and Springdoc 2.8.6; `docker compose config` passed with only obsolete-version warning |
| AI-004 | P-015 | Phase 3 authentication, authorization, and secure ownership implementation | Accepted | Human approved only `spring-boot-starter-security` and `spring-boot-starter-oauth2-resource-server`; no explicit JOSE dependency; RS256 resource server with `NimbusJwtDecoder`; `CurrentOwnerProvider` retained through `SecurityCurrentOwnerProvider`; owner identity changed to UUID/email/role; refresh tokens stored as SHA-256 digests; V2 migration added without changing V1; Redis/rate limiting/observability/admin expansion deferred | `.\mvnw.cmd clean verify` passed with 45 tests; PostgreSQL Testcontainers started; Flyway applied V1 and V2; Hibernate schema validation succeeded; `.\mvnw.cmd dependency:tree` confirmed `flyway-core:11.7.2`, `flyway-database-postgresql:11.7.2`, Spring Security `6.5.0`, and transitive `spring-security-oauth2-jose:6.5.0`; `docker compose config` passed with only obsolete-version warning |
| AI-005 | P-016 | Phase 4 Redis cache-aside and click analytics implementation | Accepted | Human approved only `spring-boot-starter-data-redis`; no direct Jedis dependency; Redis values kept to versioned redirect DTOs; analytics stores sanitized metadata only; V3 migration added without changing V1/V2; physical delete defect corrected through soft delete to preserve analytics history; no Phase 5 work added | `.\mvnw.cmd clean verify` passed with 60 tests; PostgreSQL and Redis Testcontainers started; Flyway applied V1, V2, and V3; Hibernate schema validation succeeded; `.\mvnw.cmd dependency:tree` confirmed `flyway-core:11.7.2`, `flyway-database-postgresql:11.7.2`, `spring-boot-starter-data-redis:3.5.0`, Lettuce `6.5.5.RELEASE`, and no direct Jedis dependency; `docker compose config` passed with only obsolete-version warning |
| AI-006 | P-017 | Phase 5 operational readiness implementation | Edited | No new production dependencies; no migration; implemented Redis Lua rate limiting, bounded Micrometer metrics, protected Actuator metrics, health groups, correlation IDs, security headers, production analytics pepper validation, k6 scripts, runbook, rollback guide, threat model, and failure-mode tests. Edited AI output to avoid Redis readiness dependency and duplicate metric counting. | `.\mvnw.cmd clean verify` passed with 72 tests; PostgreSQL and Redis Testcontainers started; Flyway applied V1, V2, and V3; Hibernate schema validation succeeded; `.\mvnw.cmd dependency:tree` passed and confirmed Flyway modules at 11.7.2, Micrometer 1.15.0, and no added rate-limiting library; `docker compose config` passed with only obsolete-version warning; k6 unavailable |
| AI-007 | P-018 | Phase 6 final release readiness | Edited | Added JaCoCo build reporting, expanded CI workflow, removed obsolete Compose version, rewrote README, added final engineering summary, release checklist, and test-quality review; sanitized `.env.example`; removed redundant Testcontainers BOM/property; no product feature or migration changes | `.\mvnw.cmd clean verify` passed with 72 tests and JaCoCo line 83.91% / branch 63.25%; `.\mvnw.cmd dependency:tree` passed with no direct Jedis and no rate-limit library; `docker compose config` passed without warnings; secret scan found no committed private keys/tokens/peppers |
| AI-008 | P-019 | Final documentation and README synchronization | Edited | Reconciled README with actual Spring Boot, Docker Compose, Testcontainers, JaCoCo, OpenAPI, health, and environment-variable behavior; added docs index; corrected stale phase language; fixed k6 URL creation script to include required `expiresAt`; added local key filename patterns to `.gitignore`; no application feature, dependency, migration, or architecture changes | Markdown link check found no broken relative links across 40 Markdown files outside `target`; `docker compose config` passed without warnings; `docker compose up -d` and `docker compose ps` confirmed local PostgreSQL and Redis infrastructure healthy; `.\mvnw.cmd clean verify` passed with 72 tests and JaCoCo line 83.91% / branch 63.25% |
| AI-009 | P-020 | Hyperscale production evolution | Edited | Added hyperscale NFR/capacity/storage/analytics/SLO/lifecycle/cost docs; implemented configurable 8-character Base62 generation, bounded retries, collision metrics, config-driven quotas, V4 `blocked` moderation state, admin block/unblock endpoints, blocked cache DTO support, and tests. Kept distributed KV, Redis Cluster, CDN/edge, WAF, Kafka/Kinesis/Pulsar, OLAP warehouse, and multi-region as architecture-only. | `.\mvnw.cmd clean verify` passed with 84 tests and JaCoCo line 85.52% / branch 66.49%; PostgreSQL and Redis Testcontainers started; Flyway V1-V4 validated/applied; Hibernate schema validation succeeded; `.\mvnw.cmd dependency:tree` passed with no new production dependency; `docker compose config` passed without warnings |
| AI-010 | P-021 | Render live deployment configuration | Edited | Added environment-driven `prod` profile for Render/Neon/Render Key Value, shared `PORT` support, Docker JAR runtime, `.dockerignore`, expanded secret ignore patterns, deployment docs, README links, and deployment variable template. Fixed `application-local.yml` profile activation after local startup smoke exposed a Spring Boot 3.5 config defect. Bound `spring-boot:repackage` after Render logs showed the Docker runtime copied a non-executable normal JAR. Kept business logic, auth, Flyway migrations, PostgreSQL/Redis abstractions, and dependencies unchanged. | `.\mvnw.cmd clean verify` passed with 84 tests and JaCoCo line 84.44% / branch 65.41%; PostgreSQL and Redis Testcontainers started; Flyway V1-V4 validated/applied; Hibernate schema validation succeeded; `.\mvnw.cmd dependency:tree` passed with no new production dependency; `docker compose config` passed; Markdown link check passed for 54 files; `docker compose up -d` and bounded local `spring-boot:run` liveness smoke passed; `.\mvnw.cmd clean package -DskipTests` ran `spring-boot:repackage`; manifest confirmed Boot `JarLauncher` and `Start-Class`; bounded `java -jar` liveness smoke passed; Docker image build `url-shortener-render-smoke` passed |
| AI-011 | P-022 | Enterprise production evolution Stages 1-3 | Edited | Added additive `shortUrl` response derivation from `APP_PUBLIC_BASE_URL`; added V5 idempotency table and optional `Idempotency-Key` handling scoped to authenticated users; added destination update endpoint using existing `short_urls.original_url`; exposed ETag/If-Match concurrency contract for destination changes. Deferred workspace/RBAC, audit, API keys, outbox, QR, tracing, and Angular per approval. | `.\mvnw.cmd clean verify` passed with 89 tests and JaCoCo line 85.83% / branch 65.58%; PostgreSQL and Redis Testcontainers started; Flyway V1-V5 validated/applied; Hibernate schema validation succeeded; `.\mvnw.cmd dependency:tree` passed with no new production dependency; `docker compose config` passed without warnings. |
| AI-012 | P-023 | Stage 4 workspace tenant foundation and workspace RBAC | Edited | Added V6 workspace/membership migration, workspace entities/repositories/service/controller, centralized workspace authorization, default workspace creation on registration, workspace-scoped URL/analytics repository queries, workspace-aware idempotency scope, CORS header support, and Stage 4 integration tests. Edited fixture setup to avoid duplicate default workspaces and fixed explicit path-variable names. Deferred audit hooks, API keys, outbox, tracing, QR, tags, and frontend. | `.\mvnw.cmd -q -Dtest=WorkspaceControllerIntegrationTests test` passed; `.\mvnw.cmd clean verify` passed with 92 tests and JaCoCo line 84.48% / branch 63.42%; PostgreSQL and Redis Testcontainers started; Flyway V1-V6 validated/applied; Hibernate schema validation succeeded; `.\mvnw.cmd dependency:tree` passed; `docker compose config` passed. |
| AI-013 | P-024 | Stage 5 immutable enterprise audit trail | Edited | Added V7 audit migration, audit entity/enums/repository/service/query APIs, same-transaction audit hooks for URL/workspace/membership/moderation mutations, bounded/redacted metadata, explicit audit authorization, and audit tests. Edited nullable JPQL filters to dynamic JPA specifications after PostgreSQL validation failed. Deferred API keys, transactional outbox, tags/search, QR, tracing, Angular, and distributed infrastructure. | `.\mvnw.cmd clean verify` passed with 97 tests and JaCoCo line 85.62% / branch 62.80%; PostgreSQL and Redis Testcontainers started; Flyway V1-V7 validated/applied; Hibernate schema validation succeeded; `.\mvnw.cmd dependency:tree` passed with no new production dependency; `docker compose config` passed without warnings. |
| AI-014 | P-025 | Stage 6 API keys / machine-to-machine authentication | Edited | Added V8 API-key migration, workspace-bound API-key entity/repository/service/controller, header-only API-key filter, HMAC-SHA-256 digest storage, one-time raw key return, scope-based machine access to URL/analytics APIs, API-key rate-limit policy, API-key audit events, and integration tests. Edited implementation to avoid delimiter ambiguity in raw key parsing, disable servlet auto-registration of the security filter, and emit RFC7807 429 responses from the filter path. Deferred transactional outbox, tracing, QR codes, frontend, and distributed infrastructure. | `.\mvnw.cmd clean verify` passed with 105 tests and JaCoCo line 87.09% / branch 63.87%; PostgreSQL and Redis Testcontainers started; Flyway V1-V8 validated/applied; Hibernate schema validation succeeded; `.\mvnw.cmd dependency:tree` passed with no new production dependency; `docker compose config` passed without warnings. |
| AI-015 | P-026 | Stage 7 transactional outbox and durable event delivery | Edited | Added additive V9 outbox migration, same-transaction PostgreSQL domain-event publisher, URL mutation events, durable cache invalidation events, opt-in outbox analytics publisher, bounded `SKIP LOCKED` dispatcher, retry/dead-letter behavior, safe admin outbox listing, outbox metrics, and integration tests. Edited test timestamp handling to use the application UTC clock, added pre-test cleanup for suite isolation, and made admin query parameter names explicit. Deferred tracing, QR codes, frontend, external broker infrastructure, and Stage 8 work. | Focused `.\mvnw.cmd -q "-Dtest=OutboxIntegrationTests,OutboxRollbackIntegrationTests" test` passed; `.\mvnw.cmd clean verify` passed with 113 tests and JaCoCo line 85.15% / branch 60.53%; PostgreSQL and Redis Testcontainers started; Flyway V1-V9 validated/applied; Hibernate schema validation succeeded; `.\mvnw.cmd dependency:tree` passed with no new production dependency; `docker compose config` passed without warnings. |
| AI-016 | P-027 | Stage 8 campaigns, tags, search, filtering, and large-workspace UX | Edited | Added additive V10 migration, campaign/tag entities and repositories, campaign and tag APIs, URL create campaign/tags support, URL metadata ETag endpoints, destination host derivation, bounded search repository/service, normalized tag idempotency fingerprint behavior, campaign/tag audit actions, low-cardinality metrics, configuration, docs, and k6 scripts. Edited implementation after validation to fix stale test cleanup, explicit path-variable binding, missing `PUT` authorization, deterministic idempotency normalization test data, and campaignless search response mapping. Deferred Angular, QR codes, tracing, distributed search, and Stage 9 work until Stage 8 was green. | `docker info` passed; `.\mvnw.cmd -q -Dtest=OrganizationSearchIntegrationTests test` passed; `.\mvnw.cmd clean verify` passed with 118 backend tests and JaCoCo line 85.11% / branch 60.78%; PostgreSQL and Redis Testcontainers started; Flyway V1-V10 validated/applied; Hibernate schema validation succeeded; `.\mvnw.cmd dependency:tree` passed; `docker compose config` passed with a non-fatal local Docker config access warning. |
| AI-017 | P-028 | Stage 9A Angular production frontend foundation | Edited | Created independent Angular 22 frontend with runtime JSON config, typed auth/workspace API clients, Problem Details mapper, memory-only auth state, refresh single-flight, CSRF bootstrap/header handling, workspace header propagation, functional guards/interceptors, login/register forms, authenticated workspace shell, frontend docs, and CI frontend build/test steps. Edited DTOs after backend inspection and deferred URL management, campaigns/tags UI, analytics, audit, API-key, admin, Playwright/Cypress, heavy UI/state libraries, and backend auth/cookie changes. Rejected forced npm audit remediation because it would downgrade Angular CLI to 21.0.4. | `npm ci` passed; `npm test -- --watch=false` passed with 7 tests; `npm run build` passed with initial bundle 264.00 kB raw / 71.95 kB estimated transfer; `.\mvnw.cmd clean verify` passed with 118 backend tests and JaCoCo line 85.11% / branch 60.78%; `.\mvnw.cmd dependency:tree` passed; `docker compose config` passed with a non-fatal local Docker config access warning; `npm audit --audit-level=moderate` reported 3 moderate Angular CLI dev-dependency-chain findings. |
| AI-018 | P-029 | Stage 9B production link management experience | Edited | Added authenticated URL dashboard, create flow, ETag-aware URL details editor, campaign management page, typed URL/campaign/tag clients, shared UI utilities/components, query-param filters, pagination, idempotency-key retry behavior, `If-Match` mutations, stale dashboard response guard, and frontend docs. Edited request handling to ignore stale list responses and corrected shared-component bindings after Angular validation. Deferred analytics, audit, API-key, workspace-member, platform-admin, QR, custom-domain, tracing, Stage 9C, and backend contract changes. | `npm ci` passed with known Angular CLI dev-chain warnings; `npm test -- --watch=false` passed with 13 tests; `npm run build` passed with initial bundle 100.65 kB raw / 26.07 kB estimated transfer; `.\mvnw.cmd clean verify` passed with 118 backend tests and JaCoCo line 85.11% / branch 60.78%; PostgreSQL and Redis Testcontainers started; Flyway V1-V10 validated/applied; Hibernate schema validation succeeded; `.\mvnw.cmd dependency:tree` passed; `docker compose config` passed. |
| AI-019 | P-030 | Stage 9C enterprise operations frontend | Edited | Added lazy enterprise operations routes for URL analytics, workspace audit, API keys, workspace/member management, platform admin overview, moderation, platform audit, and read-only outbox. Added focused Analytics/Audit/ApiKey/Admin API clients, expanded Workspace API, safe audit metadata allowlisting, URL audit history in URL details, platform-admin navigation guard usage, and Stage 9C frontend tests/docs. Edited design to avoid chart dependencies and keep platform ADMIN separate from workspace OWNER/ADMIN. Rejected backend changes, fabricated analytics dimensions, API-key rotation/update, outbox mutation controls, and raw metadata rendering. | `npm ci` passed with known Angular CLI dev-chain warnings; `npm test -- --watch=false` passed with 21 tests; `npm run build` passed with initial bundle 103.64 kB raw / 26.71 kB estimated transfer; Stage 9C initial delta from Stage 9B is +2.99 kB raw / +0.62 kB transfer; `npm audit --audit-level=moderate` reported 3 known moderate Angular CLI dev-chain findings; `.\mvnw.cmd clean verify` passed with 118 backend tests and JaCoCo line 85.11% / branch 60.78%; PostgreSQL and Redis Testcontainers started; Flyway V1-V10 validated/applied; Hibernate schema validation succeeded; `.\mvnw.cmd dependency:tree` passed; `docker compose config` passed. |
| AI-020 | P-031 | Stage 9D production deployment hardening and final evidence | Edited | Added Render Static Site runtime config generation, frontend deployment documentation, SPA rewrite/header guidance, production smoke checklist, release checklist, live-vs-target architecture diagrams, frontend/backend rollback guidance, and CI high-severity frontend audit. Edited evidence to avoid claiming live frontend cookie/CSRF validation without a deployed frontend URL. Rejected SameSite/CSRF weakening, hash routing, Angular-in-Spring packaging, backend migrations, and new product features. Follow-up CI failure in `OutboxRollbackIntegrationTests` was fixed by replacing a brittle total-audit-row assertion with setup-count and `URL_CREATED` rollback assertions. | `.\mvnw.cmd clean verify` passed with 118 backend tests and JaCoCo line 85.11% / branch 60.78%; PostgreSQL and Redis Testcontainers started; Flyway V1-V10 validated/applied; Hibernate schema validation succeeded. `.\mvnw.cmd dependency:tree` passed; `docker compose config` passed; `npm ci`, `npm test -- --watch=false` with 21 tests, `npm run build`, and `npm run build:render` passed. `npm audit --audit-level=moderate` reported the known 3 moderate Angular CLI dev-chain findings; `npm audit --audit-level=high` passed. `git diff --check` passed; repository secret scan found no committed secrets; compiled frontend scan found no embedded secret values. Follow-up focused rollback test and full Maven verify passed after the CI correction. |

## Blocker Records

- `P-013`: Blocker was missing `flyway-database-postgresql`. Root cause was PostgreSQL support being separated from `flyway-core` in Flyway 11. Approved correction was adding `org.flywaydb:flyway-database-postgresql` as a production/runtime dependency managed by Spring Boot while retaining `flyway-core`. Validation confirmed both modules resolved to `11.7.2`, Testcontainers PostgreSQL started, Flyway applied migrations, Hibernate validation succeeded, and the Maven build passed.
- `P-015`: After Flyway started, failures were treated as migration/schema/mapping/test defects. Fixed V2/Hibernate digest type mismatch, refresh-token reuse transaction rollback, invalid-refresh cookie clearing, and test cleanup order for the V2 FK.
- `P-016`: After Flyway started, failures were treated as migration/schema/mapping/test defects. Fixed physical URL deletion conflicting with retained click events by adding soft deletion in V3, and fixed test isolation/order issues caused by analytics FKs and async persistence.
- `P-017`: No new Flyway blocker or migration was required. A readiness-policy defect was found when Redis health affected aggregate health; fixed by excluding Redis from Actuator health because Redis is an optimization and PostgreSQL remains the source of truth.
- `P-019`: No application blocker was found. Documentation verification found one artifact defect: the k6 URL creation script did not match the current create DTO because `expiresAt` was missing. The script was corrected as documentation/test-artifact alignment, not product behavior.
- `P-020`: No dependency blocker was introduced. The approved migration was limited to V4 `blocked`; broader lifecycle enums, persisted quota tables, and distributed infrastructure were rejected or deferred to architecture docs.
- `P-021`: No deployment dependency blocker was found. Render readiness required profile/config and Docker runtime changes only; Neon PostgreSQL and Render Key Value are configured through environment variables with no committed secrets.
- `P-021 Render deploy`: Render failed with `no main manifest attribute` because Maven produced a normal JAR. Root cause was that the project imports Spring Boot dependency management but does not use `spring-boot-starter-parent`, so `spring-boot:repackage` was not automatically bound. Approved correction was binding the `repackage` goal in the existing Spring Boot Maven plugin; no dependency or application behavior change was required.
- `P-022`: No dependency blocker was introduced. The approved V5 migration added idempotency storage only. The implementation kept `Idempotency-Key` optional for backward compatibility and left workspace/RBAC migration for the next reviewed stage.
- `P-023`: No dependency blocker was introduced. Stage 4 required an additive V6 migration. Implementation defects found during validation were treated as schema/API/test defects after Flyway startup: explicit controller path-variable names were added, and test fixtures were corrected to reuse default workspaces.
- `P-024`: No dependency blocker was introduced. Stage 5 required an additive V7 migration. Implementation defects found during validation were treated as API/query/test defects after Flyway startup: explicit audit controller parameter names were added, and nullable JPQL filters were replaced with dynamic JPA specifications for PostgreSQL compatibility.
- `P-025`: No production dependency blocker was introduced. Stage 6 required additive V8 API-key storage and audit-constraint expansion only. Implementation review corrected API-key filter registration and filter-local rate-limit error handling; broader API-management features remain out of scope.
- `P-026`: No production dependency blocker was introduced. Stage 7 required additive V9 outbox storage only. Implementation defects found during validation were treated as scheduling/API/test defects after Flyway startup: outbox test rows were aligned to the application UTC clock and admin outbox query parameters were made explicit for Spring MVC binding.
- `P-027`: No production dependency blocker was introduced. Stage 8 required additive V10 campaigns/tags/search storage only. Docker/Testcontainers later became available; subsequent failures after Flyway startup were treated as defects and fixed: stale cleanup table name, explicit path-variable binding, missing URL `PUT` authorization, nondeterministic idempotency test timestamp, and campaignless search mapping.
- `P-028`: No production backend dependency blocker was introduced. Stage 9A used Angular CLI-generated dependencies only. Backend API/cookie/auth changes were rejected for this substage; the frontend documents the current liveness-based CSRF bootstrap as an existing behavior rather than a new backend contract. `npm audit fix --force` was rejected because it would downgrade the approved Angular 22 CLI baseline.
- `P-029`: No backend dependency, migration, or API blocker was introduced. Stage 9B used existing Stage 8 backend contracts. The only validation issues were frontend implementation defects: stale response handling was tightened, and shared-component input names were corrected. The Angular CLI dev-dependency-chain audit warning remains tracked; forced remediation is still rejected because it downgrades Angular CLI.
- `P-030`: No backend dependency, migration, or API blocker was introduced. Stage 9C used existing enterprise backend contracts. Unsupported frontend capabilities were rejected rather than creating backend changes: API-key update/rotation, outbox mutation controls, fabricated analytics dimensions, and platform admin access based on workspace role.
- `P-031`: No backend dependency, migration, or API blocker was introduced. Stage 9D found deployment-evidence gaps rather than product defects: frontend Render Static Site runtime configuration, SPA rewrite/header documentation, release checklist, and live frontend validation steps. Cookie/CSRF policy changes were explicitly rejected until a real deployed frontend origin can be tested.
- `P-031 CI`: GitHub Actions exposed a test defect after Flyway and application startup: `OutboxRollbackIntegrationTests` asserted the total audit table count instead of URL-mutation rollback behavior. The correction kept production code unchanged and made the test order-independent.
