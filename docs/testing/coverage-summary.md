# Coverage Summary

JaCoCo is configured through the Maven plugin and runs during `verify`. The HTML report is generated at `target/site/jacoco/index.html`, with machine-readable output at `target/site/jacoco/jacoco.csv` and `target/site/jacoco/jacoco.xml`.

## Final Coverage

Generated by:

```powershell
.\mvnw.cmd clean verify
```

Results:

- Backend tests: 125
- Frontend tests: 28
- Backend line coverage: 85.07%
- Backend branch coverage: 60.15%
- Coverage gate: none configured

No hard coverage gate is configured because the project treats coverage as quality evidence, not the target. A future moderate gate could be introduced after reviewing whether it improves behavior coverage rather than encouraging shallow tests.

## High-Risk Components

Selected package coverage:

| Package | Line coverage | Branch coverage |
| --- | ---: | ---: |
| `auth.service` | 91.89% | 67.86% |
| `auth.controller` | 95.65% | 50.00% |
| `security` | 87.07% | 64.29% |
| `apikey.service` | Covered by integration tests; see report | Covered by integration tests; see report |
| `apikey.security` | Covered by integration tests; see report | Covered by integration tests; see report |
| `url.service` | 94.97% | 81.58% |
| `audit.service` | Covered by unit and integration tests; see report | Covered by unit and integration tests; see report |
| `audit.controller` | Covered by integration tests; see report | Covered by integration tests; see report |
| `outbox.service` | Covered by integration tests; see report | Covered by integration tests; see report |
| `outbox.controller` | Covered by integration tests; see report | Covered by integration tests; see report |
| `redirect.service` | 90.00% | 66.67% |
| `redirect.cache` | 86.47% | 64.52% |
| `analytics.service` | 84.69% | 62.12% |
| `common.ratelimit` | 87.78% | 62.50% |
| `common.correlation` | 95.00% | 64.29% |
| `common.metrics` | 100.00% | n/a |

## Covered Areas

- URL validation, custom aliases, expiration, collision retry/exhaustion, pagination, and owner-scoped URL operations.
- Derived full short URL responses, idempotent URL creation, idempotency replay/conflict handling, concurrent idempotent create safety, and ETag/If-Match destination update protection.
- Workspace creation/listing, default workspace creation on registration, workspace member role enforcement, workspace-scoped URL isolation, workspace analytics access, platform-admin separation from workspace admin, invalid workspace header handling, and workspace-scoped idempotency keys.
- Immutable audit trail for URL/workspace/membership/moderation mutations, audit API authorization, redacted destination metadata, idempotency replay without duplicate audit rows, stale ETag failure without audit rows, and metadata size rejection.
- Workspace-bound API-key management, one-time raw key return, digest-only storage, scope enforcement, workspace isolation, revoked/expired/malformed/dual credential rejection, API-key idempotency separation, and API-key audit attribution.
- Transactional outbox creation, same-transaction rollback behavior, PostgreSQL row claiming, stale claim recovery, retry/dead-letter handling, cache invalidation handling, safe admin inspection, and optional outbox analytics path.
- Configurable 8-character short-code generation, invalid configuration bounds, collision metrics, quota enforcement, legacy 7-character short-code resolution, administrative blocking, and blocked redirect/cache behavior.
- PostgreSQL repositories, unique constraints, Flyway migrations, and Hibernate schema validation through Testcontainers.
- RS256 JWT validation, registration/login, refresh-token rotation, reuse detection, CSRF refresh/logout, deny-by-default behavior, and ownership enforcement.
- Redis cache-aside redirect behavior, cache miss/failure fallback, after-commit invalidation, and single-flight behavior.
- Async analytics sanitization, queue pressure, retries, idempotent persistence, owner analytics, admin analytics, and soft-delete preservation.
- Redis Lua rate limiting, limiter isolation, outage fail-open/fail-closed behavior, 429 Problem Details, correlation IDs, Actuator exposure, readiness/liveness, and security headers.
- Packaged Angular/Spring Boot route split: SPA forwards for `/`, `/login`, `/register`, and `/app/**`, while `/api/v1/**`, `/r/**`, Actuator, Swagger UI, and OpenAPI remain backend routes.
- Stage 10 site experience: Flyway V11 CMS schema, public site settings/content/announcement APIs, platform-admin CMS authorization, optimistic CMS updates, HTTPS media validation, color contrast validation, announcement filtering, admin bootstrap absent/unknown/promoted/already-admin behavior, one-time `ADMIN_PROMOTED` audit event, normal registration remaining `USER`, public landing fallback branding, CMS branding API ETags, admin route guard, and no platform nav for normal users.

## Intentionally Untested Or Lower-Coverage Areas

- DTO/entity accessor boilerplate has lower value as direct unit-test target.
- The Spring Boot application entrypoint has minimal direct coverage.
- Production infrastructure behavior such as managed database failover, external secret manager integration, and centralized observability is documented but not implemented in the prototype.
- k6 performance scripts were not executed locally because k6 is not installed.
- No mutation testing or SAST/SCA plugin is configured.

## Remaining Gaps

- Branch coverage could improve around global exception fallbacks and uncommon configuration branches.
- Outbox URL mutation handler currently has intentionally low behavioral complexity because downstream integrations are deferred.
- End-to-end CI execution must be confirmed on GitHub after the branch is pushed.
- A future build-agent configuration should address Mockito's dynamic-agent warning before newer JDKs disallow dynamic agent loading by default.
